[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "asr-name-corrector"
version = "0.1.0"
description = "Phonetic post-processing for Chinese ASR name entities."
readme = "README.md"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.10"
dependencies = [
  "pypinyin>=0.49",
  "panphon>=0.20"
]

[project.urls]
Homepage = "https://example.com/asr-name-corrector"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
